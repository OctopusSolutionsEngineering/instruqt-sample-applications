apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "demo-processor.fullname" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "demo-processor.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "demo-processor.name" . }}
    spec:
      imagePullSecrets:
        {{- toYaml .Values.imagePullSecrets | nindent 8 }}
      containers:
        - name: demo-processor
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 80
          env:
            - name: ConnectionStrings__Default
              value: "{{ .Values.env.connectionString }}"
